# Example environment variables for docker-compose
# Copy to .env and edit values before running:

DERBYNET_IMAGE=jeffpiazza/derbynet_server:latest
HTTP_PORT=8050
HTTPS_PORT=8051
DATA_DIR=./data

# DerbyNet passwords (clients must use same passwords)
TIMER_PASSWORD=
RACE_CREW_PASSWORD=murphy
RACE_COORD_PASSWORD=doyourbest
PHOTO_PASSWORD=flashbulb

# Optional user mapping (on Linux hosts)
PUID=0
PGID=0
UMASK=000

